#include <stdio.h>

int main(void) {

    int t;
    scanf("%d",&t);
    while(t--){
        int a,b;
        scanf("%d%d",&a,&b);
        int res=a*5;
        if(res>=b){
            printf("YES\n");
        }
        else{
            printf("NO\n");
        }
    }
}

