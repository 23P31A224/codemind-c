#include <stdio.h>

int main(void) {
	int t;
	scanf("%d",&t);
	while(t--)
	{
	    int x,y;
	    scanf("%d %d",&x,&y);
	    if((x*100)>=(y*10))
	    printf("CLOTH\n");
	    else
	    printf("DISPOSABLE\n");
	}
	return 0;
}

    
