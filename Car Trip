#include <stdio.h>

int main(void) {
    int t;
    scanf("%d",&t);
    while(t--){
        int a,b,c,d;
        scanf("%d%d%d%d",&a,&b,&c,&d);
        if(a+b<=c+d)
        printf("\n%d",a+b);
        else
        printf("\n%d",c+d); }

	return 0;
}
