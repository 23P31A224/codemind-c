#include <stdio.h>

int main(void) {
	int t;
	scanf("%d",&t);
	while(t--){
	    int x,y,z;
	    scanf("%d %d %d",&x,&y,&z);
	    int r=10*x;
	    int res=y*z;
	   
	    if(r>=y){
	       printf("%d\n",res); 
	    }
	    else{
	        printf("%d\n",x*z*10);
	    }
	}

}

